import { Routes } from '@angular/router';
import { DessertsComponent } from './desserts/desserts.component';

export const routes: Routes = [
    {path: '', component: DessertsComponent, title: 'Desserts'}
];
